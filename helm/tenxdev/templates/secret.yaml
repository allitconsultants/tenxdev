{{/*
Example secret template - secrets should typically be managed externally
via tools like Sealed Secrets, External Secrets Operator, or Vault.
This file serves as documentation of required secrets.

To create the secret manually:
kubectl create secret generic tenxdev-secrets \
  --from-literal=sendgrid-api-key='your-api-key' \
  --from-literal=turnstile-secret-key='your-turnstile-key' \
  --from-literal=telegram-bot-token='your-bot-token' \
  --from-literal=telegram-chat-id='your-chat-id'

For production, use GCP Secret Manager (enabled via gcp.secretManagerEnabled=true)
*/}}
