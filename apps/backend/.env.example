# Server
PORT=8080
NODE_ENV=development

# CORS
CORS_ORIGIN=http://localhost:3000

# GCP Secret Manager (Optional - if set, secrets are loaded from GCP)
# When GCP_PROJECT_ID is set, the backend will pull secrets from GCP Secret Manager
# If not set, it falls back to the environment variables below
GCP_PROJECT_ID=

# SendGrid (Email) - fallback if not using GCP Secret Manager
SENDGRID_API_KEY=your-sendgrid-api-key
SENDGRID_FROM_EMAIL=hello@tenxdev.ai
SENDGRID_TO_EMAIL=team@tenxdev.ai

# Cloudflare Turnstile (Bot Protection) - fallback if not using GCP Secret Manager
TURNSTILE_SECRET_KEY=your-turnstile-secret-key

# Telegram Bot (Chat Notifications) - fallback if not using GCP Secret Manager
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-telegram-chat-id

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
